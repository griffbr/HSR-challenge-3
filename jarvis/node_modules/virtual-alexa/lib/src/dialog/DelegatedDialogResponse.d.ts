import { IResponse } from "../core/IResponse";
import { SkillResponse } from "../core/SkillResponse";
export declare class DelegatedDialogResponse implements IResponse {
    prompt?: string;
    skillResponse?: SkillResponse;
    constructor(prompt?: string, skillResponse?: SkillResponse);
}
