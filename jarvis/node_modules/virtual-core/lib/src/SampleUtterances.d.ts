import { IModel } from "./IModel";
export declare class SampleUtterances {
    private _interactionModel;
    private samples;
    setInteractionModel(interactionModel: IModel): void;
    interactionModel(): IModel;
    addSample(intent: string, sample: string): void;
    samplesForIntent(intent: string): SamplePhrase[];
    defaultUtterance(): SamplePhrase;
}
export declare class SamplePhrase {
    sampleUtterances: SampleUtterances;
    intent: string;
    phrase: string;
    private slotNames;
    private _regex;
    constructor(sampleUtterances: SampleUtterances, intent: string, phrase: string);
    slotName(index: number): string | undefined;
    slotCount(): number;
    regex(): RegExp;
    matchesUtterance(utterance: string): SamplePhraseTest;
    private phraseToRegex(phrase);
}
export declare class SamplePhraseTest {
    samplePhrase: SamplePhrase;
    private utterance;
    private slotMatches;
    private matched;
    private matchString;
    constructor(samplePhrase: SamplePhrase, utterance: string);
    matches(): boolean;
    score(): number;
    scoreSlots(): number;
    slotValues(): string[];
    private checkSlots(input, slotValues);
    private intentSchema();
    private slotTypes();
}
