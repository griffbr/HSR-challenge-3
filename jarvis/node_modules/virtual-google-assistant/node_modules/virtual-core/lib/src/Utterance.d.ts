import { IModel } from "./IModel";
import { SamplePhrase } from "./SampleUtterances";
export declare class Utterance {
    interactionModel: IModel;
    phrase: string;
    matchedSample: SamplePhrase;
    private slots;
    constructor(interactionModel: IModel, phrase: string);
    intent(): string;
    matched(): boolean;
    slot(index: number): string | undefined;
    slotByName(name: string): string | undefined;
    toJSON(): any;
    private matchIntent();
}
