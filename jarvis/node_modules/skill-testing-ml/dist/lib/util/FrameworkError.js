function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

// Framework errors are errors that are not coming from skill code
// Instead, they are coming from the skill tester - for these, we want to display them differently
//  in our output
// Specifically, we do not want to show stack traces and we want to add a different prefix to the message
module.exports = function (_Error) {
    _inherits(FrameworkError, _Error);

    function FrameworkError(message) {
        _classCallCheck(this, FrameworkError);

        return _possibleConstructorReturn(this, (FrameworkError.__proto__ || Object.getPrototypeOf(FrameworkError)).call(this, message));
    }

    return FrameworkError;
}(Error);