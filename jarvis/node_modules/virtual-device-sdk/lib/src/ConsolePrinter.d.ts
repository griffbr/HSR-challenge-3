import { IVirtualDeviceValidatorResult } from "./VirtualDeviceValidator";
export declare class ConsolePrinter {
    private static rpad;
    private static concat;
    private totalTests;
    private totalSequences;
    private totalInteractions;
    private successfulTests;
    private successfulSequences;
    private successfulInteractions;
    printResultsByFile(resultsByFile: {
        [id: string]: IVirtualDeviceValidatorResult;
    }): string;
    printResult(name: string, result: IVirtualDeviceValidatorResult, summarize?: boolean): string;
    printSummary(output: string): string;
    private sequenceHasError;
}
